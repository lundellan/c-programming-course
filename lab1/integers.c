#include <stdio.h>
int main(int argc, char** argv)
{
	int m, n;
	
	printf("Enter the first integer:");
	scanf("%d", &m);

	printf("Enter the second integer:");
        scanf("%d", &n);

	printf("m = %d, n = %d\n", m, n);
	return 0;
}
